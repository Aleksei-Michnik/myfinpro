/**
 * Phase 20 · Iteration 20.4 — the statement parser (design §4.1).
 *
 * Pure, dependency-free TypeScript shared by the web import wizard (20.5),
 * the API (`normalizeDescription`, so remembered categories are found again)
 * and the user-run connector (20.7).
 */

export * from './normalize';
export * from './parse';
export * from './presets';
export * from './types';
export * from './values';
