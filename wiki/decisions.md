# Binding decisions (checked 2026-09-24)

Read when: you are about to choose an architecture, rename an entity, change the deploy surface or re-open a question that looks unsettled. If a decision here blocks what you want, raise it — do not quietly re-decide it.

Status is **binding** unless a later row supersedes it. Dates are the date the decision was recorded in its source; `Phase 0–1` means the decision predates dated design docs (Phase 0 closed 2026-02-13, Phase 1 on 2026-03-14).

## Operating decisions (owner, 2026-09-24, from the sibling private infra repo)

| Date       | Decision                                                                                                                                                                                              | Why                                                                                                                                                                                | Where it lives                                                                                                                         | Status                                                   |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 2026-09-24 | A production deploy must never be a side effect of a merge. `deploy-production.yml` becomes `workflow_dispatch`-only, taking a `ref` and a literal confirmation input.                                | The host now carries other tenants; a merge that silently recreates infra containers (and, on an image bump, upgrades the database in place) is not an acceptable release trigger. | `.github/workflows/deploy-production.yml` (today still has `on.push.branches: [main]`), [deployment-and-ops.md](deployment-and-ops.md) | binding — **not yet implemented**                        |
| 2026-09-24 | Outbound mail goes through one shared relay on a shared Docker network; every app signs its own DKIM.                                                                                                 | One relay per project wastes an IP reputation and duplicates config; the relay's signing plugin is unusable (stream crash), so signing stays in the app.                           | `apps/api/src/mail/mail.service.ts`, `infrastructure/haraka/` (to be retired)                                                          | binding — supersedes `docs/phase-4-smtp-design.md` §1–§6 |
| 2026-09-24 | The shared edge nginx moves out of this repo into the infra repo after the WordPress cutover (infra Phase 5). Until then this repo's edge config is shared with other tenants: reload, never restart. | The edge terminates traffic for sites this repo knows nothing about.                                                                                                               | `docker-compose.shared-nginx.yml`, `infrastructure/nginx/`, `scripts/deploy.sh` step 6                                                 | binding                                                  |
| 2026-09-24 | Local development adopts the Mdock toolkit — shared local Traefik on 80/443, a hostname registry and an isolated browser profile — in its own small PR.                                               | One local edge for every project beats per-repo port juggling, and it is how the owner reviews real-data behaviour before a production change.                                     | not yet in this repo                                                                                                                   | binding — not started                                    |
| 2026-09-24 | Stack floor: MySQL 9.7, Redis 8, Node 26, pnpm 10, TypeScript 6. Never older.                                                                                                                         | Version drift between local and production produced real bugs (auth-plugin handshakes, driver incompatibilities).                                                                  | every `docker-compose*.yml`, `.nvmrc`, root `package.json`, `.claude/skills/stack-versions/SKILL.md`                                   | binding                                                  |

## Product and domain

| Date           | Decision                                                                                                                                                                                                                                                                                                                                                             | Why                                                                                                                                             | Where it lives                                                                         | Status                                                                  |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 2026-07-03     | Re-plan of everything after Phase 6: receipts move ahead of budgets, Tesseract OCR is replaced by pluggable vision-LLM extraction, and new phases are added for the product catalog (8), MCP (11) and WebMCP (17). Renumbering: Budgets 8→10, Receipts 9→7, Analytics 10→9, Bot 11→12, Mini App 12→13, Bot Receipts 13→14, Bot Analytics 14→15, LLM Assistant 15→16. | The catalog and analytics only pay off once receipts exist, and vision models made OCR obsolete.                                                | `IMPLEMENTATION-PLAN.md` §5 re-plan note, `docs/progress.md`                           | binding                                                                 |
| 2026-04-25     | One `Transaction` entity with a `direction` field (`IN`/`OUT`) replaces the originally separate income and expense phases.                                                                                                                                                                                                                                           | Two near-identical entities would have duplicated categories, attributions, schedules, comments and every UI surface.                           | `docs/phase-6-transactions-design.md`                                                  | binding                                                                 |
| 2026-07-15     | The entity is named **Transaction**, not Payment — renamed end to end (DB, shared types, API, web, docs).                                                                                                                                                                                                                                                            | "Payment" read as outgoing-only once income shared the entity.                                                                                  | commit `a89ca0f`, `docs/phase-8-receipt-intake-design.md` §8                           | binding — no `Payment` name may come back                               |
| 2026-04-25     | Recurring-parent cascades are silent and audit-logged rather than 409; child occurrences survive parent deletion as orphaned-but-valid; cancel preserves the row, delete removes it.                                                                                                                                                                                 | The user's intent on a type change is unambiguous; occurrence history stays queryable.                                                          | `docs/phase-6-transactions-design.md` "Decisions"                                      | binding                                                                 |
| 2026-04-25     | For cascade paths, queue writes happen **after** the DB transaction commits; single-row lifecycle endpoints keep the queue write inside the transaction.                                                                                                                                                                                                             | Holding Redis I/O while MySQL row locks were open deadlocked under concurrency; low-traffic single-row paths prefer strict consistency.         | `docs/phase-6-transactions-design.md` "Decisions"                                      | binding                                                                 |
| 2026-07-04     | Receipt extraction goes through a provider interface selected by `RECEIPT_EXTRACTION_PROVIDER`, with a deterministic `mock` provider as the default for dev, CI and tests; every call is retried and circuit-broken and logs provider/model/duration/usage.                                                                                                          | Keeps model choice a config decision and keeps the test suite free and deterministic.                                                           | `docs/phase-7-receipts-design.md` §2.5, `docs/runbook-llm-extraction.md`               | binding                                                                 |
| 2026-07-04     | One receipt belongs to exactly one transaction; merchants are a global registry.                                                                                                                                                                                                                                                                                     | A receipt is a transaction's proving document, not an independent object.                                                                       | `docs/phase-7-receipts-design.md` §2.2–2.3, `docs/phase-8-receipt-intake-design.md` §4 | binding (the pre-confirm intake strip, option (b), is the agreed shape) |
| 2026-07-11     | Two-layer product model: a global barcode-keyed registry (`products`, `product_aliases`) that any user may search and grow, plus strictly private purchase data via `receipt_items.product_id`.                                                                                                                                                                      | The catalog must be shareable without ever revealing who bought what.                                                                           | `docs/phase-8-products-design.md` §1.1                                                 | binding                                                                 |
| 2026-07-12     | LLM access is a per-user setting with optional BYOK; user keys live in a dedicated table, encrypted application-side (AES-256-GCM, versioned ciphertext), write-only API, single decryption boundary, redacted logs, fresh-session guard.                                                                                                                            | User API keys are credentials to the user's paid account and must never appear in a `SELECT *`, a dump or a log.                                | `docs/runbook-llm-extraction.md` §9, §9.4                                              | binding                                                                 |
| 2026-07 (8.17) | Online receipts are resolved by host-specific provider adapters that reduce the document to a compact text snapshot before the model call; an empty extraction result fails fast with guidance instead of landing silently in REVIEW.                                                                                                                                | A client-rendered receipt page returns only a shell to a server-side fetch, and raw provider JSON blew the output cap and truncated the answer. | `docs/phase-8-receipt-intake-design.md` §5                                             | binding                                                                 |
| 2026-07-17     | Analytics run on a **hybrid grain**: item rows where a confirmed receipt has items, header rows otherwise, plus a balancing row so that Σ(purchase rows) ≡ Σ(countable transaction amounts). Aggregates stay per currency (no FX); saved views are private; nothing is materialised.                                                                                 | Item-level detail where it exists without ever making totals disagree with the transaction ledger.                                              | `docs/phase-9-analytics-design.md` §1, §2.1–2.2                                        | binding                                                                 |
| 2026-07-17     | Budgets are standing definitions with auto-repeating periods; progress is **derived on read**, never stored; period boundaries resolve in the owner's timezone (ISO weeks) via one shared `resolvePeriod`; the schema is expand-only.                                                                                                                                | Stored progress goes stale the moment a transaction is edited; one period resolver keeps API and UI in agreement.                               | `docs/phase-10-budgets-design.md` §2.1–2.2, §3; `packages/shared/src/budget-period.ts` | binding                                                                 |
| Phase 6        | Money is stored and computed as integer minor units (`*_cents`) and formatted with `Intl.NumberFormat`; `dinero.js` from the plan was never adopted.                                                                                                                                                                                                                 | Integer arithmetic on cents is exact and dependency-free; the plan's library added nothing the schema needed.                                   | `packages/shared/src/types/currency.types.ts`, `apps/web/src/lib/money.ts`             | binding — supersedes `IMPLEMENTATION-PLAN.md` §3.1                      |
| Phase 6        | Realtime is advisory: a process-local event bus, no buffer or replay; clients refetch via the `resyncToken` contract.                                                                                                                                                                                                                                                | Simplicity for one process; correctness comes from refetch, not from delivery guarantees (events do not cross blue/green slots).                | `apps/api/src/realtime/`, `docs/ui-realtime-conventions.md`                            | binding until horizontal scale-out is planned                           |
| 2026-07-24     | Analytics groups by the primary category only; the category filter matches any attached category.                                                                                                                                                                                                                                                                    | Grouping by the many-to-many would fan out rows and double-count sums.                                                                          | `apps/api/src/analytics/engine/dimension-sql.ts`, `analytics-engine.service.ts`        | binding                                                                 |
| Phase 5.8      | Audit action names stay mixed (`UPPER_SNAKE_CASE` up to 5.7, `dot.notation` after) for continuity of stored records.                                                                                                                                                                                                                                                 | Renaming stored audit actions would break history queries.                                                                                      | `docs/phase-5-progress.md` §5.8                                                        | binding                                                                 |

## Platform and delivery

| Date       | Decision                                                                                                                                                                                                                  | Why                                                                                                                | Where it lives                                                               | Status                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 2026-03-06 | Deploys are blue/green: two slots per environment, a shared nginx that both environment networks reach, traffic switched by rendering the edge vhost and reloading, and N-1 images retained locally for instant rollback. | In-place restarts caused downtime, rollback was fragile, and fixed container names blocked parallel slots.         | `docs/blue-green-deployment.md`, `scripts/deploy.sh`, `scripts/rollback.sh`  | binding (the doc still says "not yet implemented" — it shipped; code wins)                                         |
| 2026-03-06 | Long-lived infrastructure (MySQL, Redis, mail) lives in a separate `*.infra.yml` stack that app deploys never recreate, and image cleanup never touches volumes.                                                          | Application releases must not be able to destroy stateful services.                                                | `docker-compose.{staging,production}.infra.yml`, `scripts/cleanup-images.sh` | binding                                                                                                            |
| Phase 0–1  | GitHub Secrets are the only source of application secrets: injected into the SSH session and read from the shell by compose; no `.env` is written on a server.                                                            | A server-side env file is a durable copy of every secret.                                                          | `.env.{staging,production}.template`, both deploy workflows                  | binding — one exception in force: the DKIM private key is written to disk, which the shared-relay decision removes |
| Phase 0–1  | Production releases are gated on staging: the latest Test Staging run must be `success` and less than 24 hours old.                                                                                                       | Staging is the only environment where the full stack is exercised end to end.                                      | `.github/workflows/deploy-production.yml` `staging-tests-check`              | binding                                                                                                            |
| Phase 0–1  | Schema changes follow expand-then-contract; migrations must be compatible with the previous release because both slots overlap.                                                                                           | A blue/green swap runs old and new code against one database.                                                      | `IMPLEMENTATION-PLAN.md` §8.3                                                | binding                                                                                                            |
| 2026-07-19 | The platform upgrade order is Node → TypeScript → MySQL, each its own commit behind a green CI + staging gate, with the database step additionally gated on a verified backup and explicit approval.                      | Only the database step is stateful and not cleanly reversible.                                                     | `docs/platform-upgrade-2026.md` §3                                           | binding — the sequencing rule applies to every future runtime bump                                                 |
| 2026-04-11 | Mail always uses the production mail domain (`MAIL_DOMAIN`) as the sender in both environments, and DKIM is signed by the application, not the relay.                                                                     | The staging domain has no SPF record and mail was rejected; the relay's signing plugin crashes the message stream. | `docker-compose.*.app.yml`, `apps/api/src/mail/mail.service.ts`              | binding                                                                                                            |

| 2026-09-26 | Production never defaults to the mock extraction provider: an unset `RECEIPT_EXTRACTION_PROVIDER` is `unconfigured` (receipts fail with a settings-facing reason); staging and dev keep the mock. Taken in the 8.11 hotfix; owner to confirm. | Production served the fixture as "recognition" for months; staging's e2e stays deterministic. |
| 2026-09-26 | A stored personal LLM key alone binds extraction (its provider + `LLM_DEFAULT_MODEL`); the shared deployment key backs explicit selections only. Taken in the 8.11 hotfix; owner to confirm. | A saved key is intent; an unreadable key must fail, never bill the deployment's key. |

## Known drift to fix, not to re-decide

- `docs/progress.md` tech-stack table says Next.js 15; `apps/web/package.json` has Next 16.
- `IMPLEMENTATION-PLAN.md` §8.6 still shows `node:24-alpine` and §8.7 a 30/12/6 retention; the Dockerfiles use `node:26-alpine` and the backup scripts keep 7 daily + 4 weekly.
- `IMPLEMENTATION-PLAN.md` §8.5 deployment notifications were never built.
- `docker-compose.{staging,production}.yml` (monolithic) are superseded by the `infra`/`app` pair and are not used by the pipeline.
