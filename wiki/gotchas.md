# Gotchas (checked 2026-09-24)

Read when: something works locally but not in CI or on a server, a symptom looks like infrastructure rather than code, or before re-diagnosing a bug this repo has already paid for.

Every row below is a defect that actually happened here. Evidence is the file or commit that proves it.

## Local environment

| Symptom                                                                                                                                                                                | Cause                                                                                                                                                                                                                                                                                                                                  | Fix                                                                                                                                                                                                                                       | Evidence                                                                                 |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `DriverAdapterError: pool timeout … active=0 idle=0` from the host while `docker exec … mysql` works                                                                                   | long-lived local MySQL container still runs an older image than the pinned `mysql:9.7`; `docker ps` shows the name, not the image                                                                                                                                                                                                      | `docker inspect --format '{{.Config.Image}}' myfinpro-mysql`; recreate from the pinned compose (destructive to local data — ask first)                                                                                                    | `.claude/skills/stack-versions/SKILL.md`                                                 |
| `pool timeout … active=0 idle=0` although `docker exec … mysql -u<app user>` works and TCP is open                                                                                     | MySQL 9.7 accounts use `caching_sha2_password`; the first plaintext login from the MariaDB driver adapter after a server (re)start hangs until the server has that account's password cached — from any successful login, e.g. the CLI                                                                                                 | log in once with the app user inside the container (`docker exec myfinpro-mysql sh -c 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" -e "select 1"'`), then retry; seen 2026-09-25 right after the local datadir was upgraded from 8.4 to 9.7 | `apps/api/src/prisma/prisma.service.ts` (`PrismaMariaDb`)                                |
| API dev container: 461 `TS2339`/`TS2305` errors about `@prisma/client`                                                                                                                 | the dev image never runs `prisma generate` (dependencies stage installs before the schema is copied) and the anonymous `node_modules` volumes hide the host's generated client                                                                                                                                                         | `docker exec myfinpro-api sh -c 'cd /app/apps/api && pnpm exec prisma generate'`, then `docker compose restart api` (the watcher ignores generated files); the anonymous volume keeps it                                                  | `infrastructure/docker/api.Dockerfile`, `docker-compose.yml`                             |
| API dev container: `Cannot find module '/app/apps/api/dist/main'` after "Found 0 errors"                                                                                               | `apps/api/tsconfig.build.tsbuildinfo` is bind-mounted from the host and says the JS is up to date, while Nest's `deleteOutDir` just deleted it — only `.d.ts` files get emitted                                                                                                                                                        | delete `apps/api/tsconfig.build.tsbuildinfo` (and `packages/shared/tsconfig.tsbuildinfo`) on the host, restart `api`                                                                                                                      | `apps/api/nest-cli.json` (`deleteOutDir`), `packages/tsconfig/base.json` (`incremental`) |
| Behind mdock every page is 502; nginx logs `connect() failed … upstream: http://172.23.x.x:3000`                                                                                       | on the shared proxy network every project's Compose registers its bare service names (`web`, `api`) as DNS aliases, so `web` resolved to another project's container                                                                                                                                                                   | fixed on `feat/mdock` (PR #51, 2026-09-25): the local nginx upstreams name the containers (`myfinpro-web`, `myfinpro-api`); other projects that join mdock need the same                                                                  | `infrastructure/nginx/conf.d/default.conf`                                               |
| Integration runs fail with `ER_CANNOT_RETRIEVE_RSA_KEY` right after restarting the DB container                                                                                        | `caching_sha2_password` RSA cache is cold; the MariaDB driver cannot complete full auth over non-TLS                                                                                                                                                                                                                                   | do one full auth (`mysql` CLI login) to warm it, or add `allowPublicKeyRetrieval` to the adapter config                                                                                                                                   | `docs/phase-9-progress.md`                                                               |
| Docker commands hit an unexpected daemon                                                                                                                                               | WSL2 with Docker Desktop exposes two contexts, `default` (unix socket) and `desktop-linux` (Windows pipe); kernel is `5.15.146.1-microsoft-standard-WSL2`                                                                                                                                                                              | `docker context ls` before blaming compose                                                                                                                                                                                                | verified 2026-09-24 via `docker context ls`, `uname -r`                                  |
| Every two-segment route (`/auth/login`, `/settings/account`, `/legal/terms`) answers Next's own 404 while single-segment routes work, on a dev server restarted after a large refactor | Turbopack's persistent cache under `apps/web/.next` outlived the branch merge; the router served stale route metadata ("application-code" 404 in the dev log, no compile line)                                                                                                                                                         | stop the server, `rm -rf apps/web/.next`, start again — seen 2026-09-25 after merging the 20.1 UI kit; not a code regression (the same tree served every route from a clean cache)                                                        | `docs/phase-20-progress.md` §20.1                                                        |
| `e2e/payments.spec.ts` fails at `page.goto('/payments')` / `payments-list*`                                                                                                            | the spec was never renamed with the 8.20 Payment → Transaction rename; the route and every hook are `transactions-*`                                                                                                                                                                                                                   | fixed on `phase/20` (route + testids); `e2e/receipts.spec.ts` additionally needs `RECEIPT_EXTRACTION_PROVIDER=mock` in the API env or it waits for a real provider                                                                        | `apps/web/e2e/payments.spec.ts`; `.claude/skills/testing/SKILL.md`                       |
| The local stack starts with stale settings                                                                                                                                             | `scripts/dev.sh` only copies `.env.example` → `.env` when the file is missing; it never updates an existing one                                                                                                                                                                                                                        | diff against `.env.example` after pulling                                                                                                                                                                                                 | `scripts/dev.sh`                                                                         |
| Local edge container name collides with the shared edge in docs/commands                                                                                                               | the local compose also names its nginx `myfinpro-nginx`                                                                                                                                                                                                                                                                                | scope `docker exec` commands by compose project                                                                                                                                                                                           | `docker-compose.yml`, `docker-compose.shared-nginx.yml`                                  |
| Behind the mdock proxy the page loads but `/_next` dev assets and the HMR socket answer 403                                                                                            | Next 16.2 blocks cross-site dev requests: the page origin is the production hostname while Next sees `Host: localhost`                                                                                                                                                                                                                 | set `allowedDevOrigins` via `MDOCK_DEV_ORIGINS` in the local `.env` (PR #51, `apps/web/next.config.ts`, `docker-compose.mdock.yml`)                                                                                                       | `apps/web/node_modules/next/dist/server/lib/router-utils/block-cross-site-dev.js`        |
| A full page load of `/transactions` in dev opens "Something went wrong" while the list still renders                                                                                   | the mount fetch is aborted right after the silent token refresh and the ref-guarded mount effect never re-issues it, so the aborted run's `undefined` opens the retry dialog; reproduced on `phase/20` without 20.3, a client-side navigation is fine; cause not yet proven (StrictMode's dev double effect is the leading hypothesis) | fixed in 20.5 — the list client ignores aborted runs (`signal.aborted` guard before opening the dialog); every new fetch-on-mount surface uses the same guard                                                                             | throwaway request traces, 2026-09-25 (`docs/phase-20-progress.md` 20.3)                  |
| After merging a track, `Cannot find module 'xlsx'` or `Property 'apiToken' does not exist on type 'PrismaService'` in a worktree that was green before                                 | `node_modules` and the generated Prisma client are per worktree; a merge that changed `pnpm-lock.yaml` or `schema.prisma` leaves the receiving worktree stale                                                                                                                                                                          | `pnpm install --frozen-lockfile` and `pnpm --filter api exec prisma generate` (with `DATABASE_URL` exported) in that worktree before judging its gate                                                                                     | phase/20 merges of 20.5 and 20.7, 2026-09-25                                             |
| `nest build` / `nest start --watch` says "Found 0 errors" yet `Cannot find module …/dist/main`                                                                                         | the API compiles incrementally and `nest-cli.json` has `deleteOutDir`: the watcher wipes `dist`, then `apps/api/tsconfig.build.tsbuildinfo` (written by any earlier `nest build` or `tsc`) says every file is current, so nothing is emitted                                                                                           | delete `apps/api/tsconfig.build.tsbuildinfo` immediately before `pnpm --filter api dev` (a `nest build` in between recreates it)                                                                                                          | phase/20 e2e run on the merged branch, 2026-09-25                                        |

## Prisma / MySQL

| Symptom                                                                         | Cause                                                                                                                                              | Fix                                                                                                                                                                   | Evidence                                                                             |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| MySQL error 1055 under `ONLY_FULL_GROUP_BY` on an analytics query               | the same expression bound twice produces a second placeholder, which MySQL treats as a different, non-grouped expression                           | build the grouped expression once and reuse the binding                                                                                                               | `docs/phase-9-progress.md`                                                           |
| `prisma migrate deploy` fails with "schema is not empty"                        | database has tables but no `_prisma_migrations` history (pre-Prisma bootstrap)                                                                     | `scripts/deploy.sh` auto-detects it and **drops a hardcoded table list** before retrying — verify that list is still correct before letting it run anywhere with data | `scripts/deploy.sh` step 4.5; `docs/phase-1-progress.md` "Prisma Migration Baseline" |
| A migration failed but the deploy reported success                              | only the baseline branch is fatal; any other `migrate deploy` failure is logged as a warning and the deploy continues                              | grep deploy logs for `Migration failure is NOT a baseline issue` after every release                                                                                  | `scripts/deploy.sh`                                                                  |
| Boot-time scheduler reconciliation logs "table does not exist"                  | the pipeline starts the new slot **first** and runs `prisma migrate deploy` afterwards, so startup code can see the old schema                     | make startup work retry (5 × 30 s, unref'd timer) instead of assuming the schema                                                                                      | commit `0150a24`                                                                     |
| Pushing a compose image bump upgraded the database                              | the deploy runs `docker compose -f *.infra.yml up -d`, so a new MySQL tag recreates the container and the datadir upgrades in place on first start | take the backup **before** the push/merge, not during                                                                                                                 | `docs/platform-upgrade-2026.md` §3                                                   |
| A migration created on a long-lived branch is skipped or re-ordered after merge | migration timestamps are hand-set and Prisma applies them lexically; a late-landing branch sorts before newer ones                                 | re-stamp the directory name to sort last before merging (`prisma-migrations`)                                                                                         | `apps/api/prisma/migrations/` names (e.g. `20260717120000` / `…121000` pair)         |
| MySQL deadlock or hang inside an integration run touching schedules             | Redis I/O (`queue.removeJobScheduler`) executed inside a Prisma `$transaction`                                                                     | do queue work after commit; `utils/schedule-cascade.ts` returns the scheduler id for the caller                                                                       | `apps/api/src/transaction/utils/schedule-cascade.ts` comment                         |
| `/products` and `/llm` endpoints vanish after a module refactor                 | `ProductModule` and `LlmModule` enter the DI graph only through `receipt.module.ts` imports, not `app.module.ts`                                   | import them explicitly where you remove the receipt import                                                                                                            | `apps/api/src/app.module.ts` vs `apps/api/src/receipt/receipt.module.ts`             |

## Realtime / SSE

| Symptom                                              | Cause                                                                                                                                               | Fix                                                                                          | Evidence                                                    |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| Tab B never shows what tab A changed                 | the stream is closed for hidden tabs and the channel is live-only — no replay, no resync, so every event during the change is lost                  | `resyncToken` bumped on every re-open after a gap; every subscribed view refetches on change | `docs/phase-6.18.1.4-realtime-rca.md` RC1; commit `6e86e58` |
| Dashboard stale even with two visible windows        | dashboard widgets had zero realtime subscriptions — only the list and detail views were wired                                                       | subscribe every surface that renders the entity, not just the one in the iteration           | `docs/phase-6.18.1.4-realtime-rca.md` RC2                   |
| Events would not cross API instances                 | `EventBus` is a process-local rxjs `Subject`; safe only because one slot is active at a time                                                        | keep it single-slot, or move to Redis pub/sub behind the same interface before scaling out   | `docs/phase-6.18.1.4-realtime-rca.md` "Latent"              |
| SSE delivers nothing but heartbeats through the edge | the generic `/api/` proxy block buffers; SSE needs its own location with `proxy_buffering off` and a long read timeout, declared **before** `/api/` | keep `location /api/v1/events/stream` first in `ssl.conf.template`                           | `infrastructure/nginx/conf.d/ssl.conf.template`             |

## Auth / cookies

| Symptom                                                         | Cause                                                                                                                                                | Fix                                                                                                                      | Evidence                                                  |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| Everything 401s ~15 minutes after login until a page reload     | the web app refreshed the token once on mount and never again; no 401 interceptor                                                                    | proactive refresh at ~80 % of TTL + retry-on-401 in the API client                                                       | `docs/phase-6.18.1.4-rca.md` RC-1; commit `a629b4a`       |
| A storm of 401s on `/events/stream`                             | the SSE provider reconnected with exponential backoff without refreshing the expired cookie                                                          | refresh first, then reconnect; give up (status `disconnected`) if refresh fails; coordinate tabs over `BroadcastChannel` | `docs/phase-6.18.1.4-rca.md` RC-2; commit `a629b4a`       |
| Refresh silently signed the user out, then revoked all sessions | refresh cookie path was `/api/v1/auth` while the edge proxies `/api`, and a legacy cookie with the old path was sent first, tripping reuse detection | cookie path `/api`, plus explicit legacy-cookie cleanup on every set/clear; refresh returns `user` too                   | `docs/phase-1-progress.md` "Silent Refresh"               |
| JWTs signed with a dev fallback secret in a live environment    | the API read `JWT_ACCESS_SECRET` while compose and CI passed `JWT_SECRET`                                                                            | one name everywhere (`JWT_SECRET`) + startup validation that throws outside development                                  | `docs/phase-1-progress.md` "Unbound GIT_SHA + JWT_SECRET" |
| Authenticated image GET returns 400 on `?v=` cache-buster       | guards run before pipes, so a 401 masked `forbidNonWhitelisted` rejecting the unknown query param                                                    | declare every client-sent query param on the DTO, even server-ignored ones                                               | commit `740723b`                                          |

## Deploy / CI

| Symptom                                                                               | Cause                                                                                                                                                                        | Fix                                                                                                                                                                  | Evidence                                                                                  |
| ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Every deploy shipped the previous release's images                                    | `source .deploy-metadata` overwrote the `IMAGE_TAG` argument                                                                                                                 | save and restore `IMAGE_TAG`/`GIT_SHA` around the `source` (present in both `deploy.sh` and `rollback.sh` — keep it)                                                 | `docs/phase-1-progress.md`; `scripts/deploy.sh`                                           |
| Deploy script aborts on an unbound variable                                           | `set -u` with `GIT_SHA` read before it is ever set                                                                                                                           | `"${GIT_SHA:-}"`                                                                                                                                                     | `docs/phase-1-progress.md`                                                                |
| Staging requests reached production containers                                        | both environments registered the same Docker DNS aliases and the shared edge is on both networks                                                                             | env-prefixed aliases (`staging-api-<slot>`, `production-api-<slot>`) in the app compose files and the nginx template                                                 | `docs/phase-1-progress.md` "DNS Collision"                                                |
| Running containers lose name resolution after a deploy                                | `docker compose up -d --force-recreate` on the shared edge destroys its DNS cache                                                                                            | never force-recreate or restart the edge; render config and `nginx -s reload`                                                                                        | `docs/phase-1-progress.md` "Nginx Force-Recreate"                                         |
| Health check flaps at ~95 % heap                                                      | V8 keeps the heap high between GC cycles                                                                                                                                     | RSS threshold (512 MB) instead of heap percentage                                                                                                                    | `apps/api/src/health/health.controller.ts`; `docs/phase-1-progress.md`                    |
| Every product image disappeared after a deploy                                        | renditions were written to the container filesystem while the DB rows survived the slot swap                                                                                 | mount a named volume and point `PRODUCT_IMAGE_STORAGE_DIR` at it (same pattern as receipts)                                                                          | commit `17b7974`                                                                          |
| Background jobs were never enqueued, silently                                         | BullMQ ≥ 5 rejects custom job ids containing `:`                                                                                                                             | dash-separated job ids                                                                                                                                               | commit `740723b`                                                                          |
| Test Staging hangs until the job timeout, blocking production                         | `playwright install` 1.59.1 stalls after the chromium download on Node 26                                                                                                    | Playwright ≥ 1.61                                                                                                                                                    | commit `0073b5a`                                                                          |
| Docker build cannot find corepack                                                     | `node:26-alpine` no longer bundles it                                                                                                                                        | `npm install -g corepack && corepack enable`; pnpm version comes from `packageManager`                                                                               | `docs/platform-upgrade-2026.md` §2                                                        |
| 41 lint warnings / rule crashes after the ESLint bump                                 | `eslint-plugin-import` is incompatible with ESLint 10                                                                                                                        | `eslint-plugin-import-x`                                                                                                                                             | `docs/phase-1-progress.md`                                                                |
| Production deploy refuses to run although CI is green                                 | the gate requires the latest `test-staging` run to be `success` **and** < 24 h old                                                                                           | dispatch `test-staging.yml`, wait, retry                                                                                                                             | `.github/workflows/deploy-production.yml` `staging-tests-check`                           |
| A slot that serves 200s is reported as failed verification and rolled back            | `curl … \| grep -q` under `set -o pipefail`: `grep -q` exits on first match, curl gets EPIPE (exit 23), the pipeline is false; bites only when the body has content early    | write the body to a file, then grep the file; log status and first bytes on failure (the WordPress repo fixed this in `e169324`)                                     | pattern present in `scripts/deploy.sh` / `rollback.sh` — verify before trusting a failure |
| The scheduled backup run is red, or the newest backup is old                          | `backup.yml` runs `check-backup-age.sh --max-age 26` last on purpose: a stale or missing backup fails the run, and that failure is the alert (there is no other one)         | read the run log (`gh run view --log-failed`), fix the cause, re-run by dispatch; a red run before a production deploy means no fresh dump beyond the pre-deploy one | `.github/workflows/backup.yml`, `docs/backup.md`                                          |
| `check-backup-age.sh --max-age 0` passed on a brand-new file (CI drill red for weeks) | the age used to be compared in whole hours, so a file younger than an hour was "0h old" and `0 > 0` never tripped                                                            | fixed 2026-09-25: the comparison is in seconds; keep it that way                                                                                                     | `scripts/backup-common.sh` `file_older_than_hours`                                        |
| Restoring a dump into the same server fails with `ERROR 3546 … GTID_PURGED`           | the servers run MySQL with GTIDs enabled and `mysqldump` then writes `SET @@GLOBAL.GTID_PURGED` into the dump; the local MySQL has GTIDs off, so the local test never saw it | dump with `--set-gtid-purged=OFF` (both `backup.sh` and the pre-deploy step do); found by the first restore drill on staging, 2026-09-25                             | `scripts/backup.sh`, `.github/workflows/deploy-production.yml`                            |
| `gh workflow run backup.yml` answers 404 "not found on the default branch"            | GitHub registers a `workflow_dispatch` workflow only once its file is on the default branch (`main`); `--ref` does not help                                                  | the first dispatch and the schedule start after the next release to `main`; until then run the server-side steps by hand (`docs/backup.md`)                          | `.github/workflows/backup.yml`                                                            |

## LLM extraction

| Symptom                                                           | Cause                                                                                                                                       | Fix                                                                                                                                | Evidence                                   |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| Every receipt extracts as "Mock Grocery / $16.60"                 | still on the deterministic `mock` provider — variable unset, or set but not redeployed                                                      | set the repo variable/secret, then redeploy (env only applies to a fresh container)                                                | `docs/runbook-llm-extraction.md` §5        |
| API container will not boot after a config change                 | an unknown `RECEIPT_EXTRACTION_PROVIDER` value fails boot by design; in production a missing `LLM_SECRETS_ENCRYPTION_KEY` also refuses boot | fix the value / set the key before deploying                                                                                       | `docs/runbook-llm-extraction.md` §8, §9.5  |
| Receipts intermittently `FAILED`, log says `circuit breaker OPEN` | repeated provider errors tripped the breaker                                                                                                | wait out the 60 s cooldown; check provider status and rate limits                                                                  | `docs/runbook-llm-extraction.md` §5        |
| An online receipt imports blank                                   | the page is a client-rendered SPA; a server-side fetch only ever sees the shell                                                             | host-specific provider adapter returning a compact text snapshot + a provider-agnostic empty-result guard that fails with guidance | `docs/phase-8-receipt-intake-design.md` §5 |
| "Non-JSON output" parse failure on a large receipt                | the raw provider document (~21 K input tokens of noise) pushed the model into the output cap and truncated the JSON mid-array               | reduce the payload to receipt essentials before the call; output cap raised 8192 → 16384                                           | `docs/phase-8-receipt-intake-design.md` §5 |

## Testing

| Symptom                                                         | Cause                                                                                                                                                                          | Fix                                                                               | Evidence                                                                      |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Green suites, broken feature                                    | tests emitted synthetic events into a mounted, visible component and asserted `publish()` was called — no test crossed the hidden-tab → publish → refocus boundary             | test the end-to-end property, not the pieces                                      | `docs/phase-6.18.1.4-realtime-rca.md` §3                                      |
| Integration tests pass locally but auth differs from production | testcontainers must use the same engine as production (`mysql:9.7`) with the removed `--default-authentication-plugin` flag dropped, so tests exercise `caching_sha2_password` | `apps/api/test/helpers/testcontainers.ts`                                         | `docs/platform-upgrade-2026.md` §4 step 3                                     |
| Backup verification passes but does not reflect the real schema | `backup-verify.yml` still creates its fixture tables by hand; the design's `prisma db push` approach was not adopted                                                           | update the workflow SQL when the schema changes, or switch it to `prisma db push` | `.github/workflows/backup-verify.yml` vs `docs/post-phase-4-design.md` Part 3 |
| Suites fail after pointing local config at a real LLM           | unit, integration and E2E suites rely on the deterministic mock                                                                                                                | keep `RECEIPT_EXTRACTION_PROVIDER=mock` locally and in CI                         | `docs/runbook-llm-extraction.md` §7                                           |
