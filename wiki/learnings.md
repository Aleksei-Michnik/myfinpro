# Learnings log

Append-only. Written by the `learn` skill: one row per finding, newest last. `Applied to` names the
agent, skill, rule, wiki page or index that was updated so the lesson sticks. Evidence is a file,
commit or command — never memory.

| Date       | Finding                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                                                  | Applied to                                                    |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 2026-09-24 | Suite bootstrapped: index + rules + 11 agents + 12 skills + 16 wiki pages, mirroring the green-fluffy layout                                                                                                                                                                                                                                        | this branch                                                                                               | everything                                                    |
| 2026-09-24 | The canonical clone (`develop`) carried untracked prior-art agents and gitignored `.kilocode/rules`; nothing was committed — `wiki/conventions.md` is now the only tracked copy of the DNA/docs/deployment rules                                                                                                                                    | `git ls-files .claude .kilocode` in both clones                                                           | `conventions`, `progress-log`, `commit-hygiene`               |
| 2026-09-24 | Design docs drift from code in several binding places (child occurrences cascade on parent delete; no on-behalf-of attribution; Telegram is HMAC not OIDC; no `dinero.js`; responses are not enveloped; no Prisma enums) — code wins, agents must read the wiki page, not only the design doc                                                       | `wiki/{transactions,auth-and-groups,architecture-map,data-model}.md` drift sections                       | routing table (wiki page mandatory), `architect`, `api-coder` |
| 2026-09-24 | Budget alert dedup `@@unique` never fires on MariaDB (NULL FKs are distinct); replacement deferred to 10.9 while the table is empty                                                                                                                                                                                                                 | `apps/api/prisma/schema.prisma` BudgetAlertEvent; `docs/phase-10-progress.md` design revision             | `analytics-and-budgets`, `data-model`, `roadmap`              |
| 2026-09-24 | Migration timestamps are hand-set and Prisma applies lexically — a late-landing branch must re-stamp; nowhere was migration serialization written down, so it is now a process rule                                                                                                                                                                 | `apps/api/prisma/migrations/` names; orchestrator prior art                                               | `prisma-migrations`, `orchestrator`, `AGENTS.md`              |
| 2026-09-24 | BullMQ job ids must not contain `:`; job schedulers live only in Redis keyed by queue name (the rename would have silently stopped recurring transactions — mitigated by re-upsert on boot); Redis I/O inside a Prisma transaction deadlocked MySQL                                                                                                 | `docs/phase-8-progress.md` §8.25 hotfixes; `transaction-schedule.service.ts`; `utils/schedule-cascade.ts` | `transactions`, `receipts-and-llm`, `gotchas`                 |
| 2026-09-24 | `llm` and `product` modules mount only through `receipt.module.ts` imports; removing that import unmounts two APIs                                                                                                                                                                                                                                  | `apps/api/src/app.module.ts` vs `receipt.module.ts`                                                       | `architecture-map`, `gotchas`                                 |
| 2026-09-24 | CI runs no integration or local e2e suite (Testcontainers and Playwright are local-only; only staging suites run in CI); no coverage thresholds exist despite plan §3.5                                                                                                                                                                             | `.github/workflows/ci.yml`, `apps/api/jest.config.js`                                                     | `testing` (wiki + skill), `qa-tester`                         |
| 2026-09-24 | Dark mode is OS-only via `prefers-color-scheme`; no `data-theme`, no switch (the green-fluffy pattern is different — do not port claims between repos)                                                                                                                                                                                              | `apps/web/src/app/globals.css`, `[locale]/layout.tsx`                                                     | `ui-design-system`, `ui-conventions`                          |
| 2026-09-24 | Work shipped on `develop` after the last progress update (multi-category transactions, OFF import + nightly sweep, persisted extraction reasoning, discount-line fix) has no journal entry and no plan row; `docs/progress.md` understates phases 8 and 10                                                                                          | `git log develop` after 2026-07-20 vs `docs/progress.md`                                                  | `roadmap`, owner report                                       |
| 2026-09-24 | Analytics `GROUP BY`/`ORDER BY` must reuse select aliases: repeating the `CONVERT_TZ` expression binds a second placeholder and MySQL 1055 rejects it; category _dimension_ is primary-only while the category _filter_ is any-match                                                                                                                | `apps/api/src/analytics/engine/analytics-engine.service.ts`, `dimension-sql.ts`                           | `analytics-and-budgets`, `gotchas`                            |
| 2026-09-24 | Sibling repos moved today: Mdock toolkit committed with this project pre-registered; WordPress production is dispatch-only and its pipefail verification bug is fixed — ours are still pending                                                                                                                                                      | infra repo `05b3a45`; WordPress repo `f764e3e`, `e169324`                                                 | `infra-context`, `devops`, `release`                          |
| 2026-09-24 | `scripts/deploy.sh` warns and continues when `prisma migrate deploy` fails for any reason other than the baseline case, and starts the slot before migrating — the most dangerous deploy behaviour found; supports dispatch-only production                                                                                                         | `scripts/deploy.sh` step 4.5; commit `0150a24`                                                            | `deployment-and-ops`, `gotchas`, `devops`, `release`          |
| 2026-09-24 | Two secrets-at-rest exceptions exist on the host (DKIM private key written by the deploy workflows; `infrastructure/backup/backup.env` sourced by backup scripts) — both retire with the shared relay and the infra Phase 5 backup unification                                                                                                      | `.github/workflows/deploy-*.yml`, `infrastructure/backup/`                                                | `deployment-and-ops`, `decisions`, owner report               |
| 2026-09-24 | Several source docs name the deploy user, the GHCR owner path and a staging hostname (`docs/server-setup-guide.md`, `docs/runbook-llm-extraction.md` §4a, `docs/blue-green-deployment.md`, `docs/phase-4-progress.md`, `scripts/cleanup-images.sh`, env templates) — already public history, not copied into the wiki; owner to decide on scrubbing | leak-scan report of the wiki authors                                                                      | owner report                                                  |
| 2026-09-24 | Mdock adoption arrived as PR #51 from the infra session (opt-in overlay, `MDOCK_DEV_ORIGINS` for Next 16.2's cross-site dev block) — the wiki must be refreshed when a sibling session opens a PR here                                                                                                                                              | `gh pr view 51`, `origin/feat/mdock`                                                                      | `infra-context`, `gotchas`                                    |
| 2026-09-24 | Production backups may not be running: no pre-deploy dump in the workflow (verified), no crontab and empty backup dirs on the host (reported by the infra session, unverified here)                                                                                                                                                                 | `.github/workflows/deploy-production.yml`; infra runbook §5, §7                                           | `deployment-and-ops`, `gotchas`, owner report                 |
